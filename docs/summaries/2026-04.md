# 2026-04 Summary

- Menu bar controls now apply immediately after Preferences changes without requiring relaunch, and the menu switches Agent Cockpit/Agent Sessions actions between Open and Hide based on whether each window is visible.
- Update onboarding now starts with a random pair of Power Tips, fresh-install onboarding keeps the two highest-value tips with a full-tour link, and Help has a Power Tips item for reopening the multi-slide tips tour.
- Onboarding now includes a Power Tips slide in update and fresh-install tours, highlighting Hide Dock icon and Agent Cockpit.
- Archived Codex Desktop sessions now show the existing `desk` surface pill in italic.
- Claude generated `ai-title` metadata now improves Sessions-list titles when no explicit `/rename` title exists.
- Session-format monitoring now includes Hermes canonical JSON sessions and excludes legacy Droid from active checks.
- Gemini CLI 0.40 JSONL sessions under `~/.gemini/tmp/<project>/chats/session-*.jsonl` are now discovered, parsed, captured, and monitored alongside legacy Gemini JSON sessions.
- Agent format monitoring is verified through Codex 0.125.0, Claude 2.1.123, Gemini 0.40.0, Copilot 1.0.39, OpenCode 1.14.29, and OpenClaw 2026.4.26.
- Session view now has a floating top up-arrow for jumping to the first real user prompt, and user prompt blocks use a softer gray background in light mode while preserving the previous prompt text color.
- JSON-style tool outputs now render simple results, entries, files, grouped search matches, accessibility trees, error envelopes, suggestions, and trailing hints as readable transcript text instead of raw pretty-printed JSON.
- Session view first-click transcript loading now avoids duplicate selected-session parses, defers nonessential cache/inline-image/probe work, and cuts synchronous terminal linkification work so medium Codex rollouts render with less UI blocking.
- Launch and manual session refreshes now use the interactive indexing profile, so forced catch-up scans after an index reset do not crawl under the active-window throttle.
- Codex subagent session pills now expose recorded reasoning effort in their hover tooltip without adding another visible row badge.
- Crash report exports now preserve exception details, timestamps, and top frames from pretty-printed macOS `.ips` diagnostic payloads.
- Codex Resume in Terminal now launches plain `codex resume` again, with Cockpit live tracking decoupled to its process/iTerm discovery path.
- Codex VS Code-surface sessions no longer present Codex CLI resume actions when the CLI would open without useful session content.
- Codex Desktop subagent sessions now keep the `desk` Agent-column badge and show their subagent marker in the session title.
- Codex local rollout sessions now distinguish CLI, Desktop, VS Code, and subagent surfaces while remaining in one Codex search/resume corpus.
- Codex titles can now fall back to local `state_*.sqlite` thread metadata when `session_index.jsonl` has no custom thread name.
- Preferences sidebar now hides the deprecated Droid pane, while legacy Droid import support remains available in the app.
- Session list agent-name accents now separate Hermes and OpenClaw more clearly from Claude without changing Claude’s existing color.
- Hermes sessions are now supported end-to-end in Agent Sessions, including local discovery, transcript viewing, Unified list filters, and analytics visibility.
- Hermes local sessions now decode from real Hermes JSON payloads, appear immediately after enabling the provider, participate in Unified transcript search, and copy the bare Hermes resume ID instead of the `session_*.json` filename stem.
- Hermes sessions now preserve recorded `cwd` metadata for path filtering/search without probing the filesystem.
- Hermes Project labels now show the session origin recorded in `platform`, such as `cli`, `telegram`, or `cron`.
- OpenClaw Project labels now show derived session origin, such as `telegram`, `cron`, or `tui`.
- Unified Sessions can now resume Hermes sessions or copy a Hermes resume command directly from the session list.
- Cursor sessions now support Resume in Unified Sessions and Copy Resume Command generation with `--resume <chatId>` and `--continue` fallback behavior.
- Analytics indexing is now fully on-demand from the Analytics window. Unified toolbar open no longer triggers automatic builds, and users can start/cancel/update builds with continuous progress visibility.
- Unified/Cockpit responsiveness is prioritized by removing automatic analytics indexing from provider refresh paths when analytics is idle.
- Unified core indexing now runs in a lower-impact foreground profile, slows focused-session live polling, and keeps in-flight refreshes running across app deactivation instead of repeatedly canceling work.
- Unified toolbar indexing affordances now clearly separate core session indexing from analytics indexing so active refresh indicators are less confusing.
- Unified now pauses automatic core monitor refresh loops while the app is active in foreground, then resumes background monitoring when inactive to reduce active-window CPU pressure.
- Unified footer now reports live core indexing progress (`X/Y` sessions and percentage) during active indexing.
- Agent toolbar filters in Unified are now view-only and no longer kick off background re-indexing when toggled.
- Core monitor-triggered refresh work now displays as lightweight syncing status instead of resetting full indexing progress.
- Core restart sync now restores persisted file-stat baselines per source so relaunches no longer re-process large session sets when only a few files changed.
- Codex incremental refresh now detects and reparses recent rollout files that are present on disk but missing from the hydrated snapshot, fixing newest-session dropouts caused by baseline/session mismatch.
- Codex `thread_name` title overrides are now constrained to verified session-root layouts and refreshed with stronger cache invalidation, reducing wrong-title carryover across custom roots and rename edits.
- Cockpit `Go to Session` now allows runtime-ID-backed matches again while still blocking cwd-only fallback matches.
- Session list flat view now shows an `s` badge on subagent sessions so they are identifiable without expanding the hierarchy.
- Session list subagent fallback badges now live in the Session column across providers, keeping the CLI Agent column to provider names only.
- Session list hierarchy now infers same-workspace parents for older Codex role-only subagent sessions even after multi-hour gaps, reducing stray top-level `s Codex` rows in hierarchical mode.
- Cockpit Quota Meter auto-expands to show the full detail panel when any quota is amber/red and reset times no longer fit inline.
- Claude Usage now keeps a bounded timed retry active while Cockpit, footer, or menu usage surfaces are visible after OAuth failures, so limits do not wait for Claude terminal prompt activity.
- Custom session titles now supported across Claude (via `/rename` CLI command), Codex (thread_name from session_index.jsonl), Copilot (workspace name from workspace.yaml), and OpenCode (stored session titles from SQLite/JSON, including `/rename`), with proper caching and layout guards per agent.
- Fixed: Claude custom titles from `/rename` were lost on relaunch due to missing column in UPDATE; fixed alongside a gap-scan fix that catches title records in the middle of large session files.
- Resume launch now passes terminal commands to AppleScript via `osascript` argv, reducing command/script interpolation risk.
- Transcript rendering for Whole Session Raw/Pretty now preloads content off the SwiftUI `body` path, and transcript search cache now has bounded memory usage.
