# 2026-06 Summary

- Quota Meter rows now reserve the same projection/reset columns for Codex and Claude without changing the provider row styling, and Session Runway titles get about two-thirds of the row with middle truncation.
- Session Runway now keeps real active-session titles while still replacing synthesized placeholders with tab titles, and Claude subagent-heavy sessions no longer crowd other active sessions out of Runway discovery.
- Session Runway now trims the Quota Meter drawer to the measured session-row height and no longer paints a separate darker bottom band below the last visible session.
- Session Runway now shows active Codex sessions before token burn samples arrive, keeps just-finished sessions visible briefly, and preserves measured low-impact burn rows instead of hiding them as pending calculation.
- Session Runway horizontal bars now use relative, lightly animated scaling, so the highest-burn row no longer renders as a static full-width bar.
- Quota Meter now exposes the Codex Session Runway Auto, On, and Off selector as a toolbar pill that opens a popover, gives the cockpit view switcher (Full/Compact/Quota Meter) a matching popover pill to free toolbar space, makes "On" surface a real active-session row when one exists (placeholder otherwise), removes the separate EQ strip, and keeps the animated burn visualization inline with each runway row.
- The always-on limits surface is now named Quota Meter across onboarding, Settings, marketing copy, docs, and assets.
- GitHub Pages now uses the redesigned Agent Sessions landing page, with updated Antigravity positioning, Quota Meter copy, guide links, and production 3.9.3 download metadata.
- Antigravity markdown artifacts in the same conversation now keep separate session identities while resume/copy commands still target the parent conversation, stay visible when low-message filtering is enabled, infer projects from local file links, and local screenshots linked from Antigravity markdown now surface in transcript/image-browser flows.
- Gemini CLI support has been replaced by Antigravity CLI support using `agy`; old Gemini JSON/JSONL logs are ignored, and Antigravity markdown artifacts under `~/.gemini/antigravity/brain` are discovered as Antigravity sessions.
- Antigravity resume and live-session handling now use `agy --conversation <id>` with a `--continue` fallback, plus active-process and iTerm title detection for running `agy` sessions.
- Transcript pane now shows the current session name in a compact identity strip with `side`/`sub` labels and parent context for side chats, helping identify the displayed transcript when the session list is not focused.
- Side-chat rows now use synthetic side-chat paths and a `side` pill with parent context, so `#side` results no longer look like missing Codex app sessions or inherit the full SQLite log database size.
- Claude OAuth/Web usage refreshes now keep a recent hard-probe 5h limit and reset time when the soft snapshot only contains weekly quota data.
- Claude Limit Alerts now use fractional OAuth/Web usage for projected-exhaustion burn-rate checks, so real usage movement is not missed while the displayed rounded percent stays unchanged.
- Claude usage hard refresh now finds Homebrew `tmux` from GUI-launched Agent Sessions, treats Claude Code 2.x `/usage` screens without quota bars or rate-limit responses as unavailable quota data instead of false 0% limits, and points stale Claude Code CLI credentials to `claude /login`.
- Claude Code installs that use the built-in `~/.local` updater are now recognized by the per-agent update action, which can run `claude update` after confirmation.
- Side-chat rows now expose Copy Session ID when Codex logs include the parent thread id, and the transcript/list actions copy the parent main-session id.
- Side-chat recovery now keeps already discovered side-chat rows visible across partial SQLite refreshes and cache writes, so a large or briefly locked Codex log scan does not collapse `#side` back to only the root-log result.
- Unified Window no longer compares full transcript payload text during SwiftUI session diffing, preventing the main Sessions window from hanging on large Codex histories.
- Unified Window no longer parses raw transcript JSON for branch/repo metadata while drawing the Project column, preventing foreground-return table hangs on large side-chat results.
- Unified Window now caches row-derived hierarchy state during table rebuilds and defers monitor-triggered focused transcript reloads while inactive, reducing foreground-return render churn.
- Unified Window now has visible collapse-all and expand-all hierarchy buttons, plus persisted collapsed hierarchy state across relaunches.
- Side-chat recovery now publishes the main Unified Window refresh before the cache-backed side-chat pass, and bounded row-id discovery with historical backfill plus nested `~/.codex/sqlite` log discovery keeps large Codex log databases from blocking the final refresh.
- Unified Window search now supports `#side` as a fast side-chat filter tag, including combined searches such as `#side ABRACADABRA`.
- `#side` searches now ignore the archived-only Codex toggle so recovered side chats remain visible while archive filtering is enabled.
- Side-chat recovery now preserves request-only prompts as searchable titles and refreshes active `#side` results when side-chat rows arrive after the initial session list.
- Unified Window can now recover Codex side chats from Codex Desktop logs as searchable side-chat rows with readable transcripts and a distinct `side` badge, without treating them as subagents.
- Agent Cockpit Runway now adds a compact session burn detail below the Quota Meter, keeping the existing Quota Meter rows unchanged while surfacing the top active Codex consumers even when the 5h window is reset-first.
- Agent Cockpit Runway now prefers observed 5h limit movement over token-share estimates and avoids synthetic burn rates when only the reset clock is available.
- Agent Cockpit Runway labels now prefer Codex app/indexed titles and CLI `/rename` names before falling back to prompts, tab titles, or project directories, and recent Codex subagent logs now roll up under their parent session instead of appearing as duplicate Runway consumers.
- Agent Cockpit Runway now keeps worktree session identities when logs also contain source-session metadata, and nested subagent burn rolls into the worktree parent instead of showing duplicate rows.
- Unified Window can now collapse or expand all visible session hierarchy groups from the View menu.
- The Dock tile right-click menu now includes Hide Dock Icon, using the same safety path as the Settings and menu bar controls.
- Hide Dock icon can now use a pinned Agent Cockpit as the app's reopen path, so it no longer forces the menu bar item on when Cockpit is pinned.
- Turning off the menu bar item now persists across relaunches, with Agent Sessions restoring the Dock path instead of re-enabling the menu bar for reachability.
- Fresh-install and update onboarding now use a Quota Meter slide with prediction markers and Limit Alerts guidance, without adding extra steps to either tour.
- Unified Window can now hide the Transcript Window and remember that layout choice across relaunches.
- Transcript Markdown export now preserves visible images by writing embedded payloads beside the Markdown file and linking image attachments from the transcript.
- Transcript Markdown export now creates a human-readable document with metadata, User/Assistant sections, and collapsible tool details instead of terminal-style formatting.
- Transcript Markdown export now writes the selected terminal-mode session body instead of producing an empty file.
- Unified Window now labels the session count with Saved only when the saved-session filter is active.
- Unified Window now keeps the session list's scroll position stable while live-session refreshes update active indicators.
- Unified Window collapse/expand commands now stay scoped to visible hierarchy groups and preserve parent selection when collapsing a selected child row.
- Agent format monitoring now reports explicit compatibility verdicts, and Hermes parsing now keeps empty session metadata records visible as metadata events.
- The new Quota Meter gives MacBook users a tiny always-on Codex and Claude usage window when they need 5h and weekly limits visible without spending menu bar space or opening the full Agent Cockpit.
- Agent format validation now runs real-session prebump probes for OpenCode, OpenClaw, Cursor, and Hermes, and the 2026-06-02 verified coverage includes fresh real-session evidence for every configured active agent.
- Cursor format monitoring now uses the official Cursor CLI installer as a latest-version source and records fresh Cursor Desktop agent-window chat metadata.
- Claude 2.1.161, Gemini 0.45.0, and Copilot 1.0.59 now have fresh real-session format evidence.
- Agent Sessions now restores the Dock icon when Hide Dock icon is enabled but macOS cannot fit the menu bar item, preventing a hidden app from becoming unreachable.
- Hide Dock icon now uses bundle-level UIElement support and clears stale Dock recent-app entries when switching hidden, so macOS no longer leaves Agent Sessions visible in the Dock.
- The menu bar now keeps the live status label visible instead of collapsing to a generic stack icon when usage or session counts exceed a narrow width threshold.
- Agent Cockpit Quota Meter mode now shows Codex and Claude on separate rows with reset times when width allows, and uses a simpler hover toolbar.
- Codex and Claude usage tracking now share 5h and weekly limit notifications, with controls for providers, approaching-limit warnings, exhausted-limit warnings, 5h reset alerts, visual banners, and sound.
- Quota Meter now refreshes visible Codex and Claude non-tmux usage sources every 60 seconds, keeps Claude OAuth/Web cache and retry freshness within a 3-minute ceiling, repaints reset countdowns without requiring hover, shows a compact 5h projected run-out token in Cockpit, and warns with an ETA when fresh usage burn rate projects a limit exhaustion before reset.
- Cockpit 5h run-out tokens now show fresh before-reset ETAs beyond the last-hour alert window, so visible usage velocity can surface earlier than notification warnings.
- Limit Alerts now have their own Settings pane with source/freshness diagnostics, a separate predicted run-out toggle, and freshness-aware alert rules so stale snapshots do not trigger usage warnings.
- Quota Meter 5h run-out tokens now expire from the last actual burn observation instead of passive refreshes, hide when the 5h reset will arrive before the projected run-out, Claude can project from fractional usage changes before the rounded percent moves, Cockpit auto-expands when width fallback would hide an active projection, the token is bolder and higher-contrast, and Limit Alerts diagnostics now show why a 5h projection is or is not visible plus whether notification delivery queued, failed, or was blocked.
- Limit Alerts now describe low and projected exhaustion as one usage-pressure event, including percent left, fresh burn ETA when available, and reset timing, and Settings now leads diagnostics with a plain readiness verdict.
- Visible app copy now standardizes on Settings instead of mixing Settings and Preferences labels.
